Exploratory scripts that informed zmyth design decisions (silent hook
injection, OSC probe variants, Ink rendering repros). Not part of CI; run
manually against a built `zig-out/bin/zmyth`. Kept for provenance — see each
script's docstring for what it demonstrates.
